# 豆包电脑版 安装器指令（完整排盘版）

> 供 `INSTALL.md` 的自动安装协议调用。宿主：豆包电脑版。能力：完整排盘（`full`）。
> 真机已确认豆包可导入技能并执行本地 Node 脚本，因此装完整包（含预构建 `engine.mjs`）。

## AI 执行步骤（优先使用豆包原生导入）

1. 从清单取 `ming-engine-doubao.zip` 的 `downloadUrl` 与 `sha256`；下载并校验 SHA-256。
2. 用豆包的技能导入能力导入该包（`SKILL.md` + `scripts/` + `references/`，含引擎）。
3. 若无法直接调起导入界面：**不要输出十几步手动说明**，只给用户一个最小动作，例如：
   > 请点击此处确认导入 ming-engine-doubao.zip
   > 用户确认后，继续自动完成剩余步骤（启用技能 + 自检）。
4. 启用技能，刷新技能列表。
5. 自检：用下方一句话触发一次，返回三盘（西方 / 八字 / 紫微）即成功。

## 权限（最小化）

- 仅申请导入、技能目录写入、执行引擎（本地 `node`）所必需的权限；不要索取无关权限。
- 需要本地 Node.js ≥ 22。

## 自检指令

> 男，公历 1990-06-15 14:20（时间准确，示例·虚构人物，非真实个人信息），出生地 示例城市，时区 Asia/Shanghai，纬度 30.00、经度 120.00，帮我看事业。

## 一句话反馈模板

> 已安装完整排盘版（豆包）。直接说需求即可排盘 / 解读；卸载说“帮我卸载 Ming Engine”。

## 更新 / 卸载

- 更新：说“帮我更新 Ming Engine”，AI 重新导入最新版覆盖。
- 卸载：说“帮我卸载 Ming Engine”，或在豆包技能管理中删除该技能。
